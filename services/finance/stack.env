HOMELAB_ROOT=/homelab
HOMELAB_NETWORK=homelab-network

STACK_NAME=finance
STACK_NETWORK=${HOMELAB_NETWORK}
STACK_ROOT=${HOMELAB_ROOT}/${STACK_NAME}

WALLOS_HOSTNAME=wallos
WALLOS_NAME=${STACK_NAME}-${WALLOS_HOSTNAME}
WALLOS_ROOT=${STACK_ROOT}/${WALLOS_HOSTNAME}
